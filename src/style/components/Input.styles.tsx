import { StyleSheet } from "react-native";

const inputStyles = StyleSheet.create({
    inputMain: {
        width: "100%",
        borderWidth: 1,
        borderColor: '#CDCDCD',
        borderRadius: 6,
        padding: 16,
        fontWeight: "700"
    },

    inputMd: {
        padding: 16,
    },

    inputLg: {
        padding: 32
    }
});

export default inputStyles;